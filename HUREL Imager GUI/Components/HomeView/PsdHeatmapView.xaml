<UserControl x:Class="HUREL_Imager_GUI.Components.PsdHeatmapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converter="clr-namespace:HUREL_Imager_GUI.Converters"
             xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:vmdl="clr-namespace:HUREL_Imager_GUI.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="200"
             d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=vmdl:SpectrumViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <converter:BoolToVisibilityConverter x:Key="ConvBoolToVisibilityReverse" TrueValue="Collapsed" FalseValue="Visible"/>
    </UserControl.Resources>
    <!-- 부모 SpectrumView의 DataContext가 SpectrumViewModel이면 그대로 상속. 객체 탐지 모드일 때는 숨김 -->
    <scottplot:WpfPlot x:Name="PsdWpfPlot"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       MinHeight="160"
                       Visibility="{Binding DataContext.IsObjectDetectionMode, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource ConvBoolToVisibilityReverse}, FallbackValue=Visible}"/>
</UserControl>
